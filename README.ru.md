# JitsiBot Server - Бот для записи аудио с Jitsi Meet

JitsiBot Server - это инструмент автоматизации на Go, который управляет несколькими ботами для присоединения к конференциям Jitsi Meet и записи аудио всех участников. Он использует автоматизацию Chrome через chromedp для взаимодействия с веб-интерфейсом Jitsi Meet и захватывает аудиопотоки удаленных участников. Система включает веб-интерфейс для мониторинга статуса ботов и просмотра скриншотов в реальном времени.

## Возможности

- 🎤 **Автоматическая запись аудио** всех участников конференции
- 🔍 **Мониторинг в реальном времени** присоединения/выхода участников
- 💾 **Автоматическая организация файлов** по пользователям и комнатам
- 🎯 **Формат WebM** с кодеком Opus для эффективного хранения
- 🔧 **Настраиваемый** через YAML конфигурацию
- 🛡️ **Автоматизация Chrome в headless-режиме** для надежной работы
- 🔐 **Гибкая аутентификация** - поддержка логина/пароля и JWT
- 🌐 **Веб-интерфейс** для мониторинга статуса ботов и скриншотов в реальном времени
- 🚀 **Управление несколькими ботами** для одновременных записей
- 🎨 **Множественные режимы отображения** - карточки и табличный вид с сохранением выбора
- 🔒 **Опциональная BasicAuth** для защиты веб-консоли
- ⚡ **Управление жизненным циклом ботов** - остановка и перезапуск ботов через веб-интерфейс

## Как это работает

### 1. Автоматизация браузера
Go-приложение использует `chromedp` для автоматизации Chrome:
- Переходит на сервер Jitsi Meet
- Присоединяется к указанной комнате конференции
- Обрабатывает аутентификацию (логин/пароль или JWT)
- Внедряет пользовательский JavaScript для записи аудио

### 2. Захват аудио
Внедренный JavaScript (`script.js`):
- Мониторит DOM для audio-элементов с префиксом `remoteAudio_`
- Создает пользовательские Web Components для каждого участника
- Использует API `MediaRecorder` для захвата аудиопотоков
- Конвертирует аудио-чанки в base64 и отправляет в Go-бэкенд

### 3. Хранение данных
Go-бэкенд:
- Получает аудиоданные через JavaScript binding
- Декодирует base64 аудио-чанки
- Организует файлы по ID пользователя и комнаты
- Сохраняет WebM аудиофайлы с метаданными

## Установка

### Предварительные требования

1. **Go** (версия 1.24.4 или новее)
2. **Chrome/Chromium** браузер
3. **Node.js** и **npm** (для сборки веб-интерфейса)

### Зависимости

Проект использует следующие ключевые Go-зависимости:
- `github.com/chromedp/chromedp` - автоматизация Chrome
- `github.com/chromedp/cdproto` - Chrome DevTools Protocol
- `github.com/golang-jwt/jwt/v5` - генерация и подпись JWT токенов
- `github.com/gin-gonic/gin` - HTTP веб-фреймворк
- `gopkg.in/yaml.v2` - парсинг YAML конфигурации

### Сборка из исходного кода

```bash
# Собрать полное приложение (включает веб-интерфейс)
make all

# Или собрать пошагово:
make docs      # Сгенерировать документацию API
make build-ui  # Собрать React веб-интерфейс
make ssjitsi   # Собрать основной серверный бинарник

# Очистить артефакты сборки
make clean
```

Процесс сборки создаст бинарник `ssjitsi`, который включает встроенный веб-интерфейс.

## Использование

### Серверный режим (несколько ботов)

```bash
./ssjitsi -config ssjitsi.yaml
```

Сервер включает встроенный веб-интерфейс, доступный по адресу: http://localhost:8080/

### Параметры командной строки

#### Server (Несколько ботов)

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `-config` | Путь к файлу конфигурации | `ssjitsi.yaml` |
| `-help` | Показать справку | `false` |

### Примеры

**Запустить сервер с несколькими ботами:**
```bash
./ssjitsi -config ssjitsi.yaml
```

**Доступ к веб-интерфейсу:**
Откройте http://localhost:8080/ в браузере для мониторинга статуса ботов и просмотра скриншотов.

### Формат файла конфигурации

Сервер использует YAML файл конфигурации для управления несколькими ботами. Бот поддерживает два метода аутентификации:

#### Традиционная аутентификация (логин/пароль)

Для серверов Jitsi Meet с базовой аутентификацией:

```yaml
http: ":8080"

bots:
  - Room: "conference-room-1"
    BotName: "Recording Bot 1"
    DataDir: "./data"
    JitsiServer: "https://meet.jit.si/"
    Username: "user"
    Pass: "password"
    Headless: true
```

#### JWT аутентификация (рекомендуется для self-hosted Jitsi)

Для серверов Jitsi Meet с JWT токен аутентификацией:

```yaml
http: ":8080"

# Опционально: BasicAuth для веб-консоли
# web_username: admin
# web_password: secret123

bots:
  - Room: "my-room"
    BotName: "JWT Bot"
    DataDir: "./data"
    JitsiServer: "https://jitsi.example.com"
    JWTAppID: "your_app_id"
    JWTAppSecret: "your_app_secret"
    Headless: true
```

**Детали JWT аутентификации:**
- Использует JSON Web Tokens (JWT) для безопасной аутентификации
- Токены генерируются автоматически с алгоритмом подписи HS256
- Токен включает claims: `iss`, `aud`, `sub`, `room`, `exp`, `context`
- Срок действия токена: 2 часа
- Бот переходит напрямую на `{JitsiServer}/{Room}?jwt={token}`

**Поля конфигурации:**

| Поле | Обязательно | Описание |
|------|-------------|----------|
| `http` | Да | Адрес для прослушивания HTTP сервера (например, ":8080") |
| `web_username` | Нет | Логин для BasicAuth веб-консоли |
| `web_password` | Нет | Пароль для BasicAuth веб-консоли |
| `Room` | Да | Название комнаты конференции |
| `BotName` | Да | Отображаемое имя бота |
| `DataDir` | Да | Директория для хранения записей |
| `JitsiServer` | Да | URL сервера Jitsi Meet |
| `Username` | Нет | Логин для базовой аутентификации |
| `Pass` | Нет | Пароль для базовой аутентификации |
| `JWTAppID` | Нет | ID приложения JWT |
| `JWTAppSecret` | Нет | Секретный ключ для подписи JWT |
| `Headless` | Да | Запуск в headless режиме (true/false) |

**Примечание:** Если указаны и JWT credentials, и Username/Pass, приоритет имеет JWT.

## Веб-интерфейс

Сервер включает встроенное React веб-приложение для мониторинга статуса ботов:

### Возможности
- 📊 **Список ботов в реальном времени** с информацией о подключении
- 🖼️ **Живые скриншоты** из браузера каждого бота
- 🔄 **Автообновление** (боты: 10с, скриншоты: 30с)
- 📱 **Адаптивный дизайн** с Bootstrap
- ⚡ **Обработка ошибок** и состояния загрузки
- 🎨 **Режимы отображения** - переключение между компактными карточками и детальной таблицей
- 🔒 **Опциональная аутентификация** - защита доступа с помощью BasicAuth
- 🎮 **Управление ботами** - остановка, перезапуск и обновление скриншотов для отдельных ботов
- 🚦 **Индикаторы статуса** - работает (зеленый), остановлен (серый), запускается/останавливается (желтый)

### Доступ
После запуска сервера откройте http://localhost:8080/ в браузере.

### Архитектура
- **Фронтенд**: React 19.1.1 с Bootstrap 5
- **Бэкенд**: Go сервер с фреймворком Gin
- **Статические файлы**: Встроены с помощью `go:embed`
- **API**: REST эндпоинты для данных ботов и скриншотов

## Формат хранения данных

Записи организованы в следующей структуре:

```
data/
└── {room-name}/                    # Директория комнаты (безопасное имя файла)
    └── {bot-session-id}/           # Директория сессии бота
        ├── {participant-user-id}_{audio-element-id}.webm     # Аудиозаписи
        ├── {participant-user-id}_{audio-element-id}.json     # Временная метка начала
        ├── {participant-user-id}.json                        # Отображаемое имя участника
        └── room.json                                         # Название комнаты
```

### Типы файлов

1. **`.webm`** - Аудиозаписи в формате WebM с кодеком Opus
2. **`.json`** - Файлы метаданных содержащие:
   - Временные метки начала (Unix миллисекунды)
   - Отображаемые имена участников
   - Информацию о комнате

### Детали структуры директорий

- **`{room-name}/`** - Директория названа по имени комнаты (очищена для безопасности файловой системы)
- **`{bot-session-id}/`** - Уникальная директория для каждой сессии бота
- **Аудиофайлы** - Названы с ID пользователя участника и ID аудио-элемента
- **Файлы метаданных** - JSON файлы с временными метками и информацией об участниках

## JavaScript компоненты

### Пользовательские Web Components

- **`ssbot-info`**: Панель статуса с информацией о подключении бота
- **`ssbot-audio`**: Компонент записи аудио для каждого участника

### Ключевые функции

- **`observeElements()`**: Мониторит изменения DOM для audio-элементов
- **`handleElementAppeared()`**: Начинает запись при присоединении участника
- **`handleElementDisappeared()`**: Останавливает запись при выходе участника
- **`window.ssbot_writeSound`**: Binding для отправки аудиоданных в Go-бэкенд

## Решение проблем

### Распространенные проблемы

1. **Chrome не найден**: Убедитесь, что Chrome/Chromium установлен
2. **Доступ запрещен**: Запустите с соответствующими правами для директории данных
3. **Аутентификация не удалась**:
   - Для логин/пароль: Проверьте правильность учетных данных
   - Для JWT: Проверьте, что `JWTAppID` и `JWTAppSecret` соответствуют конфигурации Jitsi
   - Убедитесь, что JWT аутентификация правильно настроена на сервере Jitsi
4. **Аудио не записывается**: Проверьте консоль браузера на наличие ошибок
5. **Бот присоединяется, но не записывает**:
   - Проверьте, что у других участников включено аудио
   - Убедитесь, что `script.js` правильно внедряется
   - Следите за логами Go на наличие событий binding
6. **Ошибки JWT токена**:
   - Убедитесь, что JWT secret совпадает с настроенным на сервере Jitsi
   - Проверьте, что токен не истек (по умолчанию: срок действия 2 часа)
   - Проверьте, что название комнаты совпадает с claim в токене

### Режим отладки

Установите `Headless: false` в файле конфигурации, чтобы видеть браузер в действии. Это позволит:
- Визуально подтвердить присоединение бота к конференции
- Проверить консоль браузера на наличие JavaScript ошибок
- Отслеживать сетевые запросы
- Проверить процесс аутентификации

### Проверка логов

Бот выводит полезную информацию:
```bash
2025/10/06 18:09:46 Используем JWT авторизацию
2025/10/06 18:09:46 Переходим на URL: https://test-jitsi.aisa.ru/test_aiplan?jwt=***
2025/10/06 18:09:50 Бот 1 успешно запущен (ID: bc4aa902-1172-4fd8-800a-91ac42e31592)
```

Ожидаемые сообщения консоли:
- "Failed to create local tracks" - Нормально, у бота нет микрофона
- "Video track creation failed" - Нормально, у бота нет камеры
- "APP.conference not ready yet, will retry..." - Нормально во время инициализации

## Настройка JWT аутентификации

### Для self-hosted серверов Jitsi Meet

Для использования JWT аутентификации ваш сервер Jitsi Meet должен быть настроен с поддержкой JWT. Краткий обзор:

#### Требования к серверу

1. **Конфигурация Prosody** (`/etc/prosody/conf.avail/your-domain.cfg.lua`):
   ```lua
   VirtualHost "your-domain.com"
       authentication = "token"
       app_id = "your_app_id"
       app_secret = "your_app_secret"
   ```

2. **Установка JWT плагина**:
   ```bash
   apt-get install lua-basexx lua-cjson luarocks
   luarocks install lua-sec
   ```

3. **Настройка Jitsi Meet** (`/etc/jitsi/meet/your-domain-config.js`):
   ```javascript
   var config = {
       hosts: {
           domain: 'your-domain.com',
           muc: 'conference.your-domain.com'
       },
       // ... другие настройки
   };
   ```

#### Конфигурация бота

После настройки JWT на вашем сервере Jitsi, используйте эти параметры в `ssjitsi.yaml`:

```yaml
bots:
  - Room: "test-room"
    BotName: "Recording Bot"
    DataDir: ./data
    JitsiServer: https://your-domain.com
    JWTAppID: your_app_id          # Должен совпадать с app_id в Prosody
    JWTAppSecret: your_app_secret  # Должен совпадать с app_secret в Prosody
    Headless: true
```

Бот автоматически:
1. Сгенерирует JWT токен с правильными claims
2. Подпишет его предоставленным секретом
3. Перейдет на URL комнаты с токеном: `https://your-domain.com/room?jwt=<token>`
4. Присоединится к конференции без дополнительных запросов аутентификации

Для дополнительной информации о настройке JWT в Jitsi, см.: https://jitsi.github.io/handbook/docs/devops-guide/secure-domain

## Лицензия

Лицензия MIT - подробности в файле [LICENSE](LICENSE).